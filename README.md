# Docker file for nodeJs, chrome, bower, karma-cli

This is a Dockerfile that builds an image with the following,
* nodejs 5.0.0 (you can specify the version)
* bower
* karma-cli
* chrome latest stable
